// ***** initialise git using git init command *****
// to delete git remove .git folder using this command
// rm -rf .git*

// how to see files in working area
// ls
// how to see files in staging area
// git ls-files

// ****** remove section ******
// check user name and email global and local
// git config --local user.name
// git config --global user.name
// git config --local user.email
// git config --global user.email

// git config user.name it will give the local if present and if not then global
// same for email

// ****** set username and email ******
// git config --local user.email rautraomahesh@gmail.com
// git config --local user.name mahesh

// ****** unset username and email ******
// git config --unset user.email
// git config --unset user.name

// ****** remove section ******
// git config --local --remove-section user
