// ***** git diff *****
// diff command is used in git to track the difference between the changes made on a file
// diff command takes two inputs and reflects the differences between them

// git has three areas
// working area ----- staging area ------ repository area

// git diff
// --> is used for comparing changes between working area and staging area

// git diff --staged
// --> is used for comparing changes between staging area and repository area

// git diff head
// --> is used for comparing changes between working area and repository area
